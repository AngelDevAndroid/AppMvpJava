package com.angandroid.appmvprxjava.di;

public class NotesDagger {
    /**

     Dagger 2 -->
     Dagger 2 is a compile-time android dependency injection framework that uses Java Specification
     Request 330 and Annotations. Some of the basic annotations that are used in dagger 2 are:
     @Module This annotation is used over the class which is used to construct objects and provide
     the dependencies.
     @Provides This is used over the method in the module class that will return the object.
     @Inject This is used over the fields, constructor, or method and indicate that dependencies
     are requested.
     @Component This is used over a component interface which acts as a bridge between @Module
     and @Inject. (Module class doesn't provide dependency directly to requesting class, it uses
     component interface)
     @Singleton This is used to indicate only a single instance of dependency object is created.

    **/
}
